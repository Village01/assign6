<%- include('./includes/head.ejs') %>
    <!-- <% students.forEach(student => {  %> -->
            <div class="studentBox">
                <p>Name: <%= student.name %> </p>
                <p>Age: <%= student.age %> </p>
                <p>Description: <%= student.description %></p>
                <form action="/update-student" class="update" method='PATCH' onsubmit="onsubmitHandler(event, this)">
                    <!-- onsubmitHandler(event, this): khong co cai nay thi cua may co nhieu form luc submit ko biet form nao -->
                    <div>
                        <label for="name">Student Name</label>
                        <input type="text" name="name" id="name">
                    </div>
                    <div>
                        <label for="age">Student Age</label>
                        <input type="number" name="age" id="age">
                    </div>
                    <div>
                        <label for="description">Descprition</label>
                        <textarea name="description" id="description" cols="30" rows="10"></textarea>
                    </div>
                    <input type="hidden" name="studentId" value="<%= student._id%>">                   
                    <button type="submit">Update</button>
                </form>
        </div>
        <!-- <% }) %> -->
    <script src="/js/student.js"></script>
<%- include('./includes/end.ejs') %>